<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iu">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.InfoSalaReservaActivity"></activity>
        <activity android:name=".Activities.InfoSalaDocenteActivity" />
        <activity android:name=".Activities.SolsActivity" />
        <activity android:name=".Activities.AdminActivity" />
        <activity android:name=".Activities.DispDocenteActivity" />
        <activity android:name=".Activities.DispResultadosDocenteActivity" />
        <activity android:name=".Activities.InfoDocenteActivity" />
        <activity android:name=".Activities.InfoSalaUsuarioActivity" />
        <activity android:name=".Activities.InfoUsuarioActivity" />
        <activity android:name=".Activities.DocenteActivity" />
        <activity android:name=".Activities.UsuarioActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>